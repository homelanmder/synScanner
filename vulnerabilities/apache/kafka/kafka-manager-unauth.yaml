id: kafka-manager-unauth

info:
  name: Kafka 管理面板未授权访问
  author: Paper-Pen
  severity: low
  description: Kafka 管理面板未授权访问漏洞.
  reference:
    - https://github.com/yahoo/CMAK
  metadata:
    max-request: 1
    fofa-query: app="Kafka-Manager"
  tags: misconfig,apache,kafka,unauth,exposure

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Kafka Manager</title>"

      - type: status
        status:
          - 200
