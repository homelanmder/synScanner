id: drupal-jsonapi-user-listing

info:
  name: Drupal 用户名名单
  author: wintry
  severity: medium
  description: Drupal JSON:API 通过/user/user端点检测到用户名列表
  reference:
    - https://www.drupal.org/project/drupal/issues/3240913
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
    cvss-score: 5.3
    cwe-id: CWE-200
  metadata:
    max-request: 1
    verified: true
  tags: drupal

http:
  - method: GET
    path:
      - "{{BaseURL}}/jsonapi/user/user"

    matchers-condition: and
    matchers:
      - type: regex
        regex:
          - '\{"display_name":"([A-Sa-z0-9-_]+)"\}'

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '\{"display_name":"([A-Za-z0-9-_]+)"\}'

# Enhanced by md on 2023/02/08
